<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.goals">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ThankYouActivity"></activity>
       <!-- <activity android:name=".tagForm"></activity> -->
        <activity android:name=".OagActivity" />
        <activity android:name=".SagActivity" />
        <activity android:name=".InfoLanding" /> <!-- <activity android:name=".GoalsLanding" /> -->
        <!-- <activity android:name=".Main2Activity" /> -->
        <!-- <activity android:name=".LandingActivity" /> -->
        <!-- <activity android:name=".index" /> -->
        <!-- <activity android:name=".TagActivity"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <activity android:name=".TagActivity"/>
        <activity android:name=".oagForm" />
        <activity android:name=".sagForm" />
        <activity android:name=".tagForm" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".GoalsLanding" android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
  <activity android:name=".GoalsLanding">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        -->
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>